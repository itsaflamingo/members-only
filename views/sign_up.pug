extends layout

block content
  h1 #{title}

  form(method='POST' action='')
    div.form-group
      label(for='first_name') First Name:
      input#first_name.form-control(type='text' name='first_name' value=(undefined===user ? '' : user.first_name))
      label(for='last_name') Last Name:
      input#last_name.form-control(type='text' name='last_name' value=(undefined===user ? '' : user.last_name))
      label(for='username') Username:
      input#username.form-control(type='text' name='username' value=(undefined===user ? '' : user.username))
      label(for='password') Password:
      input#password.form-control(type='password', name='password' value=(undefined===user ? '' : user.password))
      label(for='confirm_password') Confirm Password:
      input#confirm_password.form-control(type='password', name='confirm_password' value=(undefined===user ? '' : user.password))
    button.btn.btn-primary(type='submit') Submit

  if errors
   ul
    for error in errors
     li!= error.msg
